# 鲲鹏记账 · Git 基础操作快速参考

> 面向零基础项目拥有者。本项目的 Git 仓库已初始化：
> 主分支 `main`（稳定版）+ 开发分支 `develop`（日常开发在此进行）。

## 1. 为什么用 Git

把项目每个阶段的代码“拍快照”存起来：

- 改坏了可以回退到任意历史版本
- 每次改动都有记录（谁、什么时候、改了什么）
- 本地文件丢失也能从仓库恢复

## 2. 日常三连：保存一次改动

```bash
git status                # 查看当前有哪些改动
git add .                 # 把改动放入“暂存区”（准备保存）
git commit -m "说明这次改了什么"   # 正式保存为一个版本
```

建议：每完成一个功能或修复就提交一次，说明写清楚（中文即可）。

## 3. 查看历史

```bash
git log --oneline         # 简洁版历史（版本号 + 说明）
git log --stat            # 详细版（每次改动涉及哪些文件）
```

## 4. 退回存档点教程

> 存档点 = Git 提交（commit）。已为可交付版本打了标签，可直接用标签名回退：
>
> ```text
> v1.0.0  →  d92615d  鲲鹏记账 v1.0 初始版本
> v1.2.0  →  8bc50dc  分类自定义管理 + 收入逻辑修正 + 体积优化
> ```

先记住查看命令：

```bash
git log --oneline --decorate   # 查看所有存档点、分支和标签
```

以下是四种"退回"方式，按安全程度排列，**日常建议只用前两种**。

### 方式 A：只查看旧版本内容（零风险）

```bash
git show v1.0.0                                  # 查看 v1.0 改了什么
git show v1.0.0:electron/db.js                   # 查看 v1.0 某个文件的内容
```

只看不动，当前代码完全不受影响。

### 方式 B：基于旧版本开新分支（最推荐）

想回到 v1.0 继续改，又不想动现在的 develop：

```bash
git checkout -b fix-v1.0 v1.0.0   # 从 v1.0 长出一个新分支 fix-v1.0
git checkout develop              # 随时切回开发分支
```

两个分支互不干扰，随便折腾，随时切换。

### 方式 C：软回退（保留改动）

```bash
git reset --soft v1.0.0
```

当前分支指针退回 v1.0，但之后的所有改动**保留在工作区**，可重新整理再提交。
适合"后悔了想重来但不想丢代码"。

### 方式 D：硬回退（彻底回到旧版本，危险）

```bash
git reset --hard v1.0.0
```

当前分支和代码**完全变成 v1.0 的样子**，之后的提交和所有未提交改动全部消失。
这是最彻底的退回，**慎用**。

### 回退错了怎么办：reflog 后悔药

Git 会记录每一次指针移动：

```bash
git reflog                     # 列出所有操作记录
git reset --hard 8bc50dc       # 回到 v1.2.0 那个提交号
```

只要没清空 `.git`，硬回退丢掉的提交都能找回。

### ⚠️ 三条重要提醒

1. **硬回退前**先处理未提交的改动（`git commit` 或 `git stash`），
   否则会被一并抹掉。
2. 本项目没有远程仓库，退回操作只影响本机。
3. 标签只是"书签"，打标签不会改变任何代码。

## 5. 分支（可选）

项目当前分支结构：

```text
main     ← 稳定版本，随时可交付
develop  ← 日常开发分支（当前所在分支）
```

```bash
git branch                            # 查看所有分支
git checkout develop                  # 切换到开发分支
git checkout main                     # 切回主分支
git merge develop                     # 把开发分支合并回主分支
```

## 6. .gitignore 说明（自动忽略的文件）

| 条目 | 含义 |
|------|------|
| `node_modules/` | 第三方依赖包（可用 `npm install` 重新生成） |
| `build/`、`dist/` | 编译/打包产物（可重新生成） |
| `release/` | 安装包等交付产物（体积大、不入库） |
| `*.log`、`*.tmp` | 日志和临时文件 |
| `.env` | 环境变量（含 API Key，**绝不能入库**） |
| `.DS_Store`、`Thumbs.db` | 操作系统垃圾文件 |
| `.vscode/`、`.idea/` | 编辑器个人配置 |
| `*.db*` | 本地数据库文件（数据另有导出备份） |
| `*.exe`、`*.dmg` | 可执行文件/安装包 |

## 7. 安全红线

- `.env`、API Key、密码等**永远不要提交**进 Git
- 大文件（>50MB）不要入库；需要备份时用应用内的 CSV/JSON 导出
- 当前未配置远程仓库，如需备份到 GitHub/Gitee 再另行配置

## 8. 当前仓库状态（2026-08-14）

```text
8bc50dc (tag: v1.2.0, develop) feat: v1.2.0 分类自定义管理、收入逻辑修正与体积优化
d382aa6 (main) docs: 更新 Git 快速参考（main 主分支 + develop 开发分支结构）
66981dd docs: 纳入课程资料（课件/，7 篇 AI 编程课程笔记）
d4b73bb chore: 提示词记录更新（移除 API Key 明文，改为指向 .env 配置）
c98e667 docs: 补充 Git 快速参考文档并完善 .gitignore
e099190 chore: 打包配置优化与开发进度记录
d92615d (tag: v1.0.0) feat: 鲲鹏记账 v1.0 初始版本（Electron + Vue 3 + SQLite）
```
