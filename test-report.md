# 鲲鹏记账 单元测试报告

> 生成日期：2026-08-19
> 测试环境：Windows / Node.js v24.18.0 / Electron v37.10.3 / better-sqlite3（Electron 原生编译）

## 一、总览

| 项目 | 数量 |
| --- | --- |
| 测试文件 | 4 个 |
| 用例总数 | 53 个 |
| 通过 | 53 个 |
| 失败 | 0 个 |
| 成功率 | 100% |

覆盖范围：2048 游戏核心逻辑、2048 AI 算法、分类展示元数据、SQLite 数据库层（分类/记录/查询/统计/导出）。

## 二、分层测试明细

| 模块 | 测试文件 | 运行环境 | 用例数 | 通过 | 失败 | 行覆盖率 | 分支覆盖率 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2048 游戏逻辑（src/game2048/game.mjs） | test/game.test.mjs | Node | 21 | 21 | 0 | 100% | 100% |
| 2048 AI 算法（src/game2048/ai.mjs） | test/ai.test.mjs | Node | 9 | 9 | 0 | 100% | 90.32% |
| 分类展示元数据（src/utils/categoryMeta.js） | test/categoryMeta.test.mjs | Node | 5 | 5 | 0 | 100% | 100% |
| SQLite 数据库层（electron/db.js） | test/db.test.js | Electron | 18 | 18 | 0 | — | — |
| **合计** | | | **53** | **53** | **0** | **100%** | **95.65%** |

> 说明：数据库层测试使用系统临时目录中的独立数据库文件，测试结束自动清理，不影响真实用户数据（%APPDATA%\鲲鹏记账\kunpeng.db）。

## 三、覆盖功能点

### 2048 游戏逻辑（21 例）

- 网格常量、空网格生成、深拷贝、空格统计
- 随机生成新块（满盘不加块、恰好生成一个 2 或 4）
- 四个方向移动：压缩、合并、单次合并规则（四个 2 只合并成两个 4）、连锁合并禁止
- 原地无变化时 `moved=false`、得分 0
- 移动不修改原始网格
- 游戏结束判定（满盘且无相邻相同块）、胜利判定、最大块
- 新对局初始化（两个初始块、分数 0）

### 2048 AI 算法（9 例）

- 方向常量与惩罚值
- 启发式评估：空格越多分越高、最大块在角落得分更高
- 最佳方向选择：无路可走返回 -1、唯一合法方向返回正确方向、返回方向必然合法、不修改传入网格
- 完整模拟一局（同步/异步）返回合法统计

### 分类展示元数据（5 例）

- 内置一级分类图标/颜色、二级分类继承一级样式
- 未知分类兜底色板且结果稳定、空名称不抛异常
- 默认导出的 12 个预置分类结构完整、名称无重复

### SQLite 数据库层（18 例）

- 建表与预置分类初始化、重复打开幂等
- 分类新增/修改/删除：空名称、超长、重名（不区分大小写）、上级不存在、预置只读、记录引用保护
- 记录新增/查询/修改/删除：非法类型、非正金额、一级分类、不存在的分类、记录完整往返
- 分页查询：日期范围、类型、一级分类自动包含二级、LIKE 通配符转义、分页
- 最近记录倒序、月度汇总（总收支/笔数/一级分类聚合）、近 N 个月趋势
- CSV 导出（UTF-8 BOM、引号转义）、JSON 导出
- 历史收入记录迁移到“收入/其他”且幂等、清空记录

## 四、失败详情

无失败用例。

## 五、运行方式

| 命令 | 作用 |
| --- | --- |
| `npm test` | 运行全部测试（单元 + 数据库层） |
| `npm run test:unit` | 只运行纯模块单元测试（Node） |
| `npm run test:db` | 只运行数据库层测试（Electron） |
| `npm run test:coverage` | 运行单元测试并输出覆盖率 |

## 六、测试过程中的修复与说明

1. **分类模块 JSON 导入兼容性修复**：`src/utils/categoryMeta.js` 的 `import` 语句补充了 `with { type: 'json' }` 导入属性（1 行改动，非逻辑变更），使模块能在 Node 环境下直接测试，同时 Vite 构建已验证通过。
2. **测试辅助函数修正**：`test/db.test.js` 中查找二级分类时补传上级分类 ID（纯测试代码修正，不涉及业务代码）。
3. **Windows 瞬时文件锁提示**：首次连续运行数据库测试时，曾出现 better-sqlite3 原生模块被占用（Windows DLL 文件锁）的瞬时错误，重跑即通过，与应用代码无关。若再次遇到，稍等片刻重跑 `npm run test:db` 即可。

## 七、补充验收（2048 AI 实战）

运行项目自带 AI 实战脚本 `node scripts/test_ai_2048.mjs 3`：

| 局数 | 得分 | 最大块 | 步数 |
| --- | --- | --- | --- |
| 1 | 16544 | 1024 | 994 |
| 2 | 16108 | 1024 | 942 |
| 3 | 14472 | 1024 | 847 |

验收结果：3/3 局达到 1024 分，平均分 15708，验收通过 ✅
