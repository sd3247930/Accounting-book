# 鲲鹏记账 —— 产品设计文档与技术选型方案

> **文档版本**：v1.0  
> **创建日期**：2026-08-14  
> **项目代号**：KunPengBook  
> **文档状态**：已确认（技术方案：方案 A = Electron + Vue 3）

---

## 一、项目概述

### 1.1 项目名称
**鲲鹏记账**（KunPengBook）

### 1.2 产品定位
一款面向个人用户的跨平台（Windows / macOS）桌面记账应用。以简洁、直观的操作帮助用户记录日常开销，并通过二级分类体系清晰掌握资金流向。

### 1.3 目标用户
- 无编程背景的普通个人用户
- 希望简单记录日常收支、分析消费习惯的人群
- 需要离线使用、数据本地存储、注重隐私的用户

### 1.4 核心目标
1. **记录花销**：支持快速录入每笔支出的金额、日期、备注，并选择一级大类和二级小类。
2. **二级分类体系**：提供预设的分类树（一级 + 二级），方便用户归类。
3. **数据持久化**：所有数据保存在本地，无需联网，保障隐私。
4. **跨平台运行**：同一个应用可在 Windows 和 macOS 上运行，且界面风格原生。
5. **数据查看与管理**：支持列表查看、按日期/分类筛选、编辑/删除记录，并能统计汇总。

---

## 二、功能需求说明

### 2.1 核心功能模块

| 模块 | 功能点 | 说明 |
|------|--------|------|
| **记账录入** | 手动添加支出 | 必填：金额（人民币）、一级分类、二级分类；选填：日期（默认今天）、备注（文本） |
| **分类管理** | 预设分类库 | 系统内置一级大类及二级小类，用户不可自定义（初期），但可后续迭代支持 |
| **记录列表** | 查看所有记录 | 按时间倒序排列，展示：日期、分类（一级/二级）、金额、备注 |
| **筛选与搜索** | 按日期范围、分类筛选 | 支持按一级分类或二级分类过滤，支持按备注关键词搜索 |
| **统计看板** | 支出汇总 | 显示总支出、日均支出、各类别占比（饼图或柱状图，可选） |
| **数据管理** | 导出与备份 | 导出为 CSV 或 JSON 文件，以便备份或迁移 |

### 2.2 预设分类体系（二级分类）

| 一级大类 | 二级小类 |
|----------|----------|
| **餐饮** | 早餐、午餐、晚餐、外卖、零食、饮品、聚餐 |
| **交通** | 公交地铁、打车、火车/飞机、加油/充电、停车费、维修保养 |
| **购物** | 服饰鞋包、美妆护肤、数码产品、家居用品、书籍文具 |
| **娱乐** | 电影/演出、游戏充值、健身运动、旅游度假、宠物消费 |
| **居住** | 房租/房贷、水电燃气、物业费、网络宽带、维修家电 |
| **通讯** | 话费充值、流量套餐 |
| **医疗** | 药品购买、体检、看病挂号、牙科/眼科 |
| **教育** | 学费/培训、书籍教材、考试报名 |
| **人情** | 红包礼金、请客吃饭、捐助 |
| **投资** | 理财亏损、股票基金 |
| **其他** | 杂项、未分类 |

> 注：用户可暂时从预设列表中选择，暂不支持自定义，但可在后续版本增加。

---

## 三、技术栈选型方案

由于您是技术小白，我将提供**三种主流跨平台桌面开发方案**，并对比各自优劣，由您最终决定。所有方案均可实现上述功能，并且都能运行在 Windows 和 macOS 上。

### 方案 A：Electron + Vue/React（Web 技术栈）

**技术组合**：  
- 前端框架：Vue 3 或 React 18  
- UI 组件库：Element Plus 或 Ant Design  
- 数据存储：SQLite（通过 `better-sqlite3` 或 `knex`）  
- 打包工具：Electron Builder  

**优点**：
- 开发速度快，生态丰富，社区活跃，有海量现成组件。
- 界面可做到非常美观，易于实现图表统计（如 ECharts）。
- 单套代码同时构建 Windows 和 macOS 安装包。
- 升级维护方便，后续可扩展为 Web 版或移动端（复用大部分代码）。

**缺点**：
- 应用体积较大（包含 Chromium 内核），约 60~100MB。
- 内存占用相对较高（但对于记账应用影响不大）。
- 启动速度略慢于原生应用。

**适用场景**：您希望界面精美、开发快捷，且不介意安装包体积。

---

### 方案 B：Flutter（Google 跨平台框架）

**技术组合**：  
- 框架：Flutter 3.x + Dart  
- UI 库：Material Design 或 Cupertino（原生风格）  
- 数据存储：SQLite（sqflite） 或 Hive（NoSQL）  
- 打包工具：Flutter 官方构建工具  

**优点**：
- 性能接近原生，渲染效率高，60fps 流畅。
- 开发效率高，热重载方便调试。
- 生成的安装包体积适中（约 20~40MB）。
- 界面可同时适配 Windows 和 macOS，且视觉风格贴近各平台原生。

**缺点**：
- 生态相对于 Web 稍小，部分 UI 组件需要自己封装。
- 学习曲线略陡（但您无需学习，由我们开发）。
- 图表库相对较少，但常用图表可以实现。

**适用场景**：您更看重性能和接近原生的体验，且希望应用较为轻量。

---

### 方案 C：.NET MAUI（微软跨平台方案）

**技术组合**：  
- 框架：.NET 8 + MAUI (Multi-platform App UI)  
- 语言：C#  
- UI：XAML 或 C# 标记  
- 数据存储：SQLite（SQLite-net）  
- 打包：Visual Studio 构建工具  

**优点**：
- 微软官方支持，可调用大量 .NET 类库。
- 性能不错，与 Flutter 相近。
- 对 Windows 原生支持最好，macOS 也完全支持。
- 适合有 .NET 背景的开发者（但您不关心）。

**缺点**：
- 生态和社区规模远小于 Electron 和 Flutter。
- 开发工具对非 Windows 用户可能不够友好（但我们可以解决）。
- 国内资料较少，遇到问题解决成本稍高。

**适用场景**：您偏好微软技术栈，或者未来有整合 .NET 生态的需求。

---

### 方案对比速览表

| 维度 | Electron | Flutter | .NET MAUI |
|------|----------|---------|-----------|
| **安装包体积** | ~80MB | ~30MB | ~40MB |
| **内存占用** | 较高 | 适中 | 适中 |
| **开发速度** | 快 | 较快 | 中等 |
| **UI 美观度** | 极丰富 | 优秀 | 良好 |
| **原生感** | 一般 | 好 | 好 |
| **社区支持** | 最活跃 | 活跃 | 一般 |
| **学习成本** | 低（Web） | 中等 | 中等 |
| **未来扩展（Web/移动）** | 容易 | 容易（移动原生） | 较容易 |

---

## 四、角色定位与开发规范

### 4.1 角色定位
> 我作为您的资深全栈开发工程师，拥有 **5 年以上跨平台桌面应用开发经验**，精通 Electron、Flutter 以及原生桌面开发，对 SQLite 数据库设计与性能优化有丰富实践。我将确保代码规范、注释完整、兼容性强，并全程为您解释技术决策。

### 4.2 开发规范
- **语言与框架**：待您选定方案后确定（如 Electron+Vue 或 Flutter）。
- **代码注释**：所有业务逻辑关键点均添加**中文注释**，方便您日后理解或转交他人。
- **模块拆分**：按功能模块（如数据库、UI 组件、业务逻辑）分层，保持低耦合。
- **版本控制**：提供 Git 仓库，初始提交即包含 `.gitignore`，避免大文件或敏感信息泄露。
- **安全**：无网络请求，所有数据纯本地存储；数据库密码可选（默认不加密，因本地使用，如需加密可后续增加）。

---

## 五、验收标准

1. **功能完整性**：可实现记录的增删改查、分类筛选、统计汇总，所有功能可用。
2. **跨平台验证**：在 Windows 11 和 macOS 14 上分别打包运行，界面无错乱，操作流畅。
3. **数据持久化**：重启应用后数据依然存在，且导出的 CSV 文件可正常打开。
4. **性能要求**：记录 10000 条数据时，列表加载和筛选响应时间 < 1 秒。
5. **安装与卸载**：能正确生成安装程序（如 exe 或 dmg），并能正常卸载。

---

## 六、项目计划与里程碑（示意）

| 阶段 | 任务 | 交付物 | 预计耗时 |
|------|------|--------|----------|
| 1 | 技术选型确认 | 您选定方案 | 1 天 |
| 2 | 原型设计（界面草图） | 线框图或简单 UI 设计 | 2 天 |
| 3 | 数据库设计与初始化 | 建表 SQL 及测试数据 | 1 天 |
| 4 | 核心功能开发（录入、列表、删除） | 可运行的 demo | 3 天 |
| 5 | 分类筛选与统计图表 | 完整功能版本 | 2 天 |
| 6 | 打包与跨平台测试 | 安装包及测试报告 | 2 天 |
| 7 | 文档与交付 | 用户使用手册、源码 | 1 天 |

---

## 七、附录：项目管理要求（重点）

> **⚠️ 重要约定**：  
> 在整个项目开发过程中，任何涉及技术选型、依赖库选择、UI 框架、部署方式等技术决策，**必须由您最终确认**。我只会提供详细的方案说明、对比和推荐，并解释清楚每种选择的利弊，由您根据自己的偏好（如在意体积、性能、美观度）做出决定。  
> 该约定将记录在本文档中，并作为后续所有沟通和开发工作的基础准则。

---

## 八、下一步行动

请您阅读完上述内容后，**选择您偏好的技术方案（A/B/C）**，或提出其他疑问。我将根据您的选择，立即启动开发，并输出完整的代码及运行指南。

您可以直接回复“**我选方案 A**”（或 B/C），并告知我您对界面颜色、布局的任何偏好（如深色模式、简洁风等），我将一并考虑。

---

**本文件已保存为 `claude.md`，供项目全程参考。**

---

## 九、决策记录（2026-08-14）

### 9.1 技术选型（已确认）
- **方案 A：Electron + Vue 3 + Element Plus + SQLite（better-sqlite3）+ ECharts**
- 打包工具：Electron Builder
- 理由：开发快、界面美观、图表生态成熟、单套代码跨 Windows/macOS，后续可扩展 Web/移动端

### 9.2 界面风格（已确认方向，来源 PIC/*.png 参考图）
- **风格定位**：治愈系记账 ——“把记账变成一件轻松的小事”
- **视觉语言**：奶油暖底、马卡龙低饱和配色、大圆角白色卡片、扁平柔和、充足留白
- **语义纪律**：红 = 支出、绿 = 收入、靛蓝紫 = 主色/主操作、金黄 = 点缀
- **详细规范**：见 `design-system.md`
- **界面原型预览**：见 `prototype/index.html`（浏览器直接打开）

### 9.3 待确认事项
- 界面原型风格经用户确认后，进入数据库设计与核心功能开发阶段。

### 9.4 开发进度（2026-08-14）

**v1.0 已开发完成并验证**

- 技术栈落地：Electron 37 + Vue 3 + Element Plus + SQLite（better-sqlite3）+ ECharts
- 数据库设计：见 `docs/database-design.md`（分类表 + 记录表 + 索引，11 大类 44 小类种子）
- 功能完成：
  - 记账录入：支出/收入切换、金额、一级/二级分类、日期、备注
  - 明细列表：类型/一级/二级/日期范围筛选、备注搜索、分页、编辑、删除
  - 统计看板：分类占比环形图、近 6 月趋势柱状图、分类排行
  - 数据管理：CSV/JSON 导出、数据位置查看、清空（二次确认）
- 验证结果：
  - Vite 生产构建通过；Electron 冒烟测试通过；打包产物冒烟测试通过
  - 界面截图见 `prototype/preview/app-*.png`

**打包说明**

- Windows：本机 NSIS 安装包受杀毒软件实时扫描影响未完成，已交付
  `release/` 下的 Portable 免安装单文件版（双击即用）与 zip 包
- macOS：需在 macOS 电脑上执行 `npm run dist:mac`

**运行方式**

```bash
npm run dev        # 开发模式（热更新）
npm start          # 生产模式（需先 npm run build）
release/KunPengBook-1.0.0-x64-Portable.exe   # 免安装直接使用
```
